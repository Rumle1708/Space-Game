#include "stm32f30x_conf.h" // STM32 config
#include "30010_io.h" 		// Input/output library for this course
#include "stdio.h"
#include "stdint.h"
#include "main.h"
#include <string.h>
#include <math.h>

struct gravity {
	int16_t x, y;
};

struct gravity *g;

void force (*g) {
	s =
	device vector
	int32_t dist = sqrt((asteroid.x - player.x)^2 + (asteroid.y - player.y)^2);
	double d = 1/2 * a * s^2 + v0 * s + d0
}
